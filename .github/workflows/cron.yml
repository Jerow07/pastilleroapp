name: Pill Reminder Cron
on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  ping-api:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Cron API
        run: |
          curl -X GET "https://pastilleroapp.vercel.app/api/cron-notifications"

